{
  "name": "kablet",
  "private": true,
  "workspaces": [
    "apps/*"
  ],
  "scripts": {
    "dev:backend": "npm run dev --workspace=@kablet/backend",
    "dev:web": "npm run dev --workspace=web"
  }
}